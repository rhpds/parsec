{{- if and .Values.deploy .Values.namespace.create -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "mlflow.namespaceName" . }}
  labels:
    {{- include "mlflow.labels" . | nindent 4 }}
{{- end -}}
