fastapi>=0.128.0
httpx>=0.27.0
uvicorn[standard]>=0.35.0
pydantic>=2.8.0
dynaconf>=3.2.0
pyyaml>=6.0
anthropic[vertex,bedrock]>=0.40.0
boto3>=1.34.0
azure-identity>=1.15.0
azure-storage-blob>=12.19.0
azure-cosmos>=4.7.0
google-cloud-bigquery>=3.4.0
google-cloud-resource-manager>=1.12.0
mcp>=1.8.0
mlflow>=2.10.0
# Pinned, not floated: the wheel ships its own `claude` binary under
# claude_agent_sdk/_bundled/, and the SDK's _find_cli() prefers it over
# anything on PATH. So this pin — not the `npm install -g
# @anthropic-ai/claude-code@<version>` in dockerfiles/Dockerfile — decides
# which CLI actually executes with the pod's credentials. Floating it meant
# every image rebuild could silently change the runtime.
claude-agent-sdk==0.2.106
